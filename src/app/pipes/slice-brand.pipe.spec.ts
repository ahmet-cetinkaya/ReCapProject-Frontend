import { SliceBrandPipe } from './slice-brand.pipe';

describe('SliceBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
