import { FilterColorPipe } from './filter-color.pipe';

describe('FilterColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColorPipe();
    expect(pipe).toBeTruthy();
  });
});
