import { FilterBrandPipe } from './filter-brand.pipe';

describe('FilterBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
